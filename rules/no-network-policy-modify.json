{
  "rule_id": "no-network-policy-modify",
  "pack": "conduct-network-ops",
  "version": "0.1.0",
  "action": "block",
  "severity": "high",
  "surface": ["hook"],
  "description": "Autonomous agents may not modify security-sensitive network configuration (ACL, firewall, routing policy, RADIUS/AAA). Reversible operational actions (QoS, service restart, logging) remain allowed. Policy changes must go through human approval per the Level 5 autonomy tier.",
  "match": {
    "tool_name": ["Bash", "run_shell"],
    "any": [
      {
        "field": "command",
        "op": "regex",
        "value": "(set|edit|delete)\\s+(firewall|security|policy-options|routing-instances|access-list|ip access-list)"
      },
      {
        "field": "command",
        "op": "regex",
        "value": "(config(ure)?\\s+(t|terminal))|(commit\\s+confirmed)"
      },
      {
        "field": "command",
        "op": "regex",
        "value": ">\\s*(fixtures/)?recent_config_changes\\.json"
      },
      {
        "field": "command",
        "op": "regex",
        "value": "acl_modify|firewall_policy|bgp_policy|radius_config"
      }
    ]
  },
  "message": "Blocked: this action would modify a security-sensitive network policy. Autonomous agents can propose the change and log the recommendation, but the commit must be approved by a human (Level 5 in the autonomy hierarchy).",
  "guidance": "Write the recommendation to remediation.log and page the on-call engineer. If this is a genuine emergency, an authorized operator can approve the change in the Conduct console."
}
